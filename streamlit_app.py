import streamlit as st

st.set_page_config(page_title="คำนวณเงินเดือนใหม่", page_icon="💰", layout="centered")

st.title("💰 แอปคำนวณเงินเดือนใหม่")
st.write("คำนวณการขึ้นเงินเดือนตามอายุงานของพนักงาน")

# รับค่าจากผู้ใช้
salary = st.number_input("💵 เงินเดือนเดิม", min_value=0.0, format="%.2f")
years = st.number_input("📅 อายุงาน (ปี)", min_value=0.0, format="%.1f")

# ตรวจสอบอัตราเพิ่ม
if years < 1:
    rate = 0
elif years < 3:
    rate = 0.03
elif years < 5:
    rate = 0.05
else:
    rate = 0.07

# ปุ่มคำนวณ
if st.button("คำนวณเงินเดือนใหม่"):
    increase = salary * rate
    new_salary = salary + increase
    
    # แสดงผล
    st.subheader("📌 ผลลัพธ์การคำนวณ")
    st.write(f"อัตราการเพิ่มเงินเดือน: **{rate * 100:.0f}%**")
    st.write(f"จำนวนเงินที่เพิ่มขึ้น: **{increase:,.2f} บาท**")
    st.write(f"💵 **เงินเดือนใหม่: {new_salary:,.2f} บาท**")
